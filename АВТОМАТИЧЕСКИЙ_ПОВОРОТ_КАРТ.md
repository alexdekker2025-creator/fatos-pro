# 🎴 Автоматический поворот карт по времени суток

## Что изменилось

Теперь после расчета автоматически открывается карта, соответствующая текущему времени суток:

- 🌅 **Утро (06:00-11:59)** → автоматически открывается карта "Утро"
- ☀️ **День (12:00-17:59)** → автоматически открывается карта "День"
- 🌇 **Вечер (18:00-23:59)** → автоматически открывается карта "Вечер"
- 🌙 **Ночь (00:00-05:59)** → автоматически открывается карта "Ночь"

## Временные рамки

| Время суток | Часы | Карта | Emoji |
|-------------|------|-------|-------|
| Утро | 06:00 - 11:59 | morning | 🌅 |
| День | 12:00 - 17:59 | day | ☀️ |
| Вечер | 18:00 - 23:59 | evening | 🌇 |
| Ночь | 00:00 - 05:59 | night | 🌙 |

## Как это работает

### 1. Определение текущего времени
```javascript
const getCurrentTimeOfDay = () => {
  const now = new Date();
  const hours = now.getHours();
  
  if (hours >= 6 && hours < 12) return 'morning';      // 06:00-11:59
  else if (hours >= 12 && hours < 18) return 'day';    // 12:00-17:59
  else if (hours >= 18 && hours < 24) return 'evening'; // 18:00-23:59
  else return 'night';                                  // 00:00-05:59
};
```

### 2. Автоматический поворот
```javascript
useEffect(() => {
  if (!autoFlipped) {
    const timer = setTimeout(() => {
      const currentTimeOfDay = getCurrentTimeOfDay();
      setFlipped({ [currentTimeOfDay]: true });
      setAutoFlipped(true);
    }, 800); // Задержка 800ms для плавности
    
    return () => clearTimeout(timer);
  }
}, [autoFlipped]);
```

## Примеры работы

### Пример 1: Утро (08:30)
1. Пользователь открывает сайт в 08:30
2. Выполняет расчет
3. Автоматически открывается карта "Утро" 🌅
4. Показывается описание: *"Утро силы. Ты чувствуешь, что можешь всё..."*

### Пример 2: День (14:00)
1. Пользователь открывает сайт в 14:00
2. Выполняет расчет
3. Автоматически открывается карта "День" ☀️
4. Показывается описание: *"День реализации. Ты — центр событий..."*

### Пример 3: Вечер (20:00)
1. Пользователь открывает сайт в 20:00
2. Выполняет расчет
3. Автоматически открывается карта "Вечер" 🌇
4. Показывается описание: *"Вечером важно закрепить результат..."*

### Пример 4: Ночь (02:00)
1. Пользователь открывает сайт в 02:00
2. Выполняет расчет
3. Автоматически открывается карта "Ночь" 🌙
4. Показывается описание: *"Ночь удовлетворения. Ты засыпаешь с чувством..."*

## Как проверить

### Шаг 1: Проверьте текущее время
```bash
node test-time-of-day.js
```

Скрипт покажет:
- Текущее время
- Какое время суток определено
- Какая карта откроется автоматически

### Шаг 2: Откройте приложение
```
http://localhost:3001/ru
```

### Шаг 3: Выполните расчет
- Имя: **Анна**
- Дата: **15.05.1990**
- Нажмите **"Рассчитать"**

### Шаг 4: Проверьте результат
После расчета должна автоматически открыться карта, соответствующая текущему времени:
- Если сейчас утро (06:00-11:59) → откроется карта "Утро"
- Если сейчас день (12:00-17:59) → откроется карта "День"
- Если сейчас вечер (18:00-23:59) → откроется карта "Вечер"
- Если сейчас ночь (00:00-05:59) → откроется карта "Ночь"

## Технические детали

### Измененный файл
- `components/CardOfDay.tsx`

### Добавленные функции
1. `getCurrentTimeOfDay()` - определяет текущее время суток
2. Обновленный `useEffect` для автоматического переворота

### Задержка переворота
- 800 миллисекунд (0.8 секунды)
- Позволяет пользователю увидеть все 4 карты перед переворотом
- Создает плавный эффект появления

### Логирование
В консоли браузера (F12) можно увидеть:
```
[CardOfDay] Auto-flipped card: day
```

## Преимущества

✅ **Персонализация** - карта соответствует текущему моменту
✅ **Актуальность** - пользователь сразу видит релевантное описание
✅ **UX** - не нужно искать нужную карту вручную
✅ **Магия** - создает ощущение, что карты "знают" время

## Тестирование в разное время

Чтобы протестировать разные времена суток, можно:

### Вариант 1: Изменить системное время
1. Измените время на компьютере
2. Обновите страницу (F5)
3. Выполните расчет

### Вариант 2: Изменить код временно
В `components/CardOfDay.tsx` замените:
```javascript
const hours = now.getHours();
```
на:
```javascript
const hours = 8; // Тест утра (06:00-11:59)
// const hours = 14; // Тест дня (12:00-17:59)
// const hours = 20; // Тест вечера (18:00-23:59)
// const hours = 2; // Тест ночи (00:00-05:59)
```

## Совместимость

✅ Работает во всех браузерах
✅ Работает на мобильных устройствах
✅ Работает в разных часовых поясах (использует локальное время пользователя)

---

**Статус:** ✅ Реализовано и протестировано
**Файл:** components/CardOfDay.tsx
**Дата:** ${new Date().toLocaleDateString('ru-RU')} ${new Date().toLocaleTimeString('ru-RU')}
