# Настройка администратора

Руководство по назначению роли администратора пользователю в FATOS.pro.

## Обзор

Для доступа к административной панели (`/admin`) пользователь должен иметь флаг `isAdmin = true` в базе данных. Этот документ описывает процесс назначения роли администратора.

## Предварительные требования

1. PostgreSQL база данных должна быть запущена и доступна
2. Переменная окружения `DATABASE_URL` должна быть настроена
3. Пользователь должен быть зарегистрирован в системе
4. Node.js и npm должны быть установлены

## Шаг 1: Применение миграции

Сначала нужно добавить поле `isAdmin` в таблицу User.

### Автоматическая миграция (рекомендуется)

```bash
npx prisma migrate dev --name add_is_admin_field
```

### Ручная миграция

Если автоматическая миграция не работает, выполните SQL вручную:

```bash
# Подключитесь к PostgreSQL
psql -U your_username -d your_database

# Выполните SQL
ALTER TABLE "User" ADD COLUMN "isAdmin" BOOLEAN NOT NULL DEFAULT false;
```

### Генерация Prisma Client

После миграции обязательно сгенерируйте Prisma Client:

```bash
npx prisma generate
```

## Шаг 2: Назначение роли администратора

### Способ 1: Использование скрипта (рекомендуется)

#### Windows (PowerShell)

```powershell
.\scripts\make-admin.ps1 admin@fatos.pro
```

#### Linux/Mac (Bash)

```bash
chmod +x scripts/make-admin.sh
./scripts/make-admin.sh admin@fatos.pro
```

#### Напрямую через Node.js

```bash
npx ts-node scripts/make-admin.ts admin@fatos.pro
```

### Способ 2: Через SQL

```sql
-- Найти пользователя
SELECT id, email, name, "isAdmin" FROM "User" WHERE email = 'admin@fatos.pro';

-- Назначить роль администратора
UPDATE "User" SET "isAdmin" = true WHERE email = 'admin@fatos.pro';

-- Проверить результат
SELECT id, email, name, "isAdmin" FROM "User" WHERE email = 'admin@fatos.pro';
```

### Способ 3: Через Prisma Studio

```bash
npx prisma studio
```

1. Откройте таблицу `User`
2. Найдите нужного пользователя
3. Измените поле `isAdmin` на `true`
4. Сохраните изменения

## Шаг 3: Проверка доступа

1. Войдите в систему под учетной записью администратора
2. Перейдите на `/admin` (или `/ru/admin`, `/en/admin`)
3. Вы должны увидеть административную панель

Если доступ запрещен:
- Проверьте, что поле `isAdmin = true` в базе данных
- Проверьте, что вы вошли под правильной учетной записью
- Очистите кеш браузера и cookies
- Перезапустите сервер разработки

## Примеры использования скрипта

### Назначить администратора

```bash
# Windows
.\scripts\make-admin.ps1 admin@fatos.pro

# Linux/Mac
./scripts/make-admin.sh admin@fatos.pro
```

**Вывод:**
```
🔍 Поиск пользователя с email: admin@fatos.pro...
✅ Успешно! Пользователь назначен администратором:
   ID: clx123abc456
   Имя: Admin User
   Email: admin@fatos.pro
   Администратор: Да

🎉 Теперь пользователь может получить доступ к /admin
```

### Проверка существующего администратора

Если пользователь уже является администратором:

```
🔍 Поиск пользователя с email: admin@fatos.pro...
ℹ️  Пользователь Admin User (admin@fatos.pro) уже является администратором
```

### Ошибка: пользователь не найден

Если пользователь не зарегистрирован:

```
🔍 Поиск пользователя с email: notfound@example.com...
❌ Пользователь с email notfound@example.com не найден!

💡 Убедитесь, что:
   1. Email указан правильно
   2. Пользователь зарегистрирован в системе
```

## Снятие роли администратора

Если нужно снять роль администратора:

### Через SQL

```sql
UPDATE "User" SET "isAdmin" = false WHERE email = 'admin@fatos.pro';
```

### Через Prisma Studio

1. Откройте `npx prisma studio`
2. Найдите пользователя
3. Измените `isAdmin` на `false`

## Множественные администраторы

Вы можете назначить несколько администраторов:

```bash
# Первый администратор
npx ts-node scripts/make-admin.ts admin1@fatos.pro

# Второй администратор
npx ts-node scripts/make-admin.ts admin2@fatos.pro

# Третий администратор
npx ts-node scripts/make-admin.ts admin3@fatos.pro
```

## Проверка списка администраторов

### Через SQL

```sql
SELECT id, email, name, "createdAt" 
FROM "User" 
WHERE "isAdmin" = true 
ORDER BY "createdAt" DESC;
```

### Через Prisma Studio

1. Откройте `npx prisma studio`
2. Перейдите в таблицу `User`
3. Добавьте фильтр: `isAdmin = true`

## Безопасность

### Рекомендации

1. **Ограничьте количество администраторов**
   - Назначайте роль только доверенным лицам
   - Регулярно проверяйте список администраторов

2. **Используйте сильные пароли**
   - Минимум 12 символов
   - Комбинация букв, цифр и символов
   - Уникальный пароль для каждого администратора

3. **Мониторинг действий**
   - Все действия администраторов логируются
   - Регулярно проверяйте логи в `/admin` → вкладка "Логи"

4. **Двухфакторная аутентификация**
   - Рекомендуется добавить 2FA для администраторов (будущая функция)

### Логирование

Все действия администраторов автоматически логируются в таблицу `AdminLog`:
- Создание статей
- Обновление статей
- Удаление статей

Просмотр логов доступен в административной панели на вкладке "Логи".

## Устранение неполадок

### Ошибка: "Cannot find module '@prisma/client'"

```bash
npm install @prisma/client
npx prisma generate
```

### Ошибка: "Database connection failed"

Проверьте переменную окружения `DATABASE_URL` в `.env`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/fatos_pro"
```

### Ошибка: "Column 'isAdmin' does not exist"

Примените миграцию:

```bash
npx prisma migrate dev --name add_is_admin_field
```

### Скрипт не запускается на Windows

Убедитесь, что политика выполнения PowerShell разрешает запуск скриптов:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Скрипт не запускается на Linux/Mac

Добавьте права на выполнение:

```bash
chmod +x scripts/make-admin.sh
```

## Альтернативный метод (устаревший)

В предыдущих версиях использовалась переменная окружения `ADMIN_EMAILS`. Этот метод больше не рекомендуется, но все еще работает как fallback:

```env
# .env (устаревший метод)
ADMIN_EMAILS=admin1@fatos.pro,admin2@fatos.pro
```

**Рекомендация:** Используйте поле `isAdmin` в базе данных вместо `ADMIN_EMAILS`.

## Дополнительные ресурсы

- [Документация по административной панели](./admin-api-endpoints.md)
- [Документация по AdminService](../lib/services/admin/README.md)
- [Prisma документация](https://www.prisma.io/docs/)

## Поддержка

Если у вас возникли проблемы:
1. Проверьте логи сервера
2. Проверьте подключение к базе данных
3. Убедитесь, что все миграции применены
4. Проверьте права доступа к файлам скриптов
