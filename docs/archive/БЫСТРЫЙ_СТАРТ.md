# 🚀 Быстрый старт: Advanced Authentication

## ✅ Что готово

Система расширенной аутентификации **полностью реализована** и готова к использованию!

### Функции
- ✅ Восстановление пароля (email)
- ✅ Подтверждение email
- ✅ Двухфакторная аутентификация (2FA)
- ✅ OAuth (Google, Facebook)
- ✅ Админ панель со статистикой
- ✅ Автоматическая очистка токенов и логов

---

## 📋 Что нужно сделать перед деплоем

### 1. Настроить Resend (для email)

1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. Создайте API key
3. Добавьте и верифицируйте ваш домен
4. Сохраните API key для Vercel

### 2. Настроить OAuth (опционально)

**Google:**
1. [Google Cloud Console](https://console.cloud.google.com)
2. Создайте OAuth 2.0 credentials
3. Добавьте redirect URI: `https://yourdomain.com/api/auth/oauth/google/callback`

**Facebook:**
1. [Facebook Developers](https://developers.facebook.com)
2. Создайте приложение
3. Добавьте redirect URI: `https://yourdomain.com/api/auth/oauth/facebook/callback`

### 3. Сгенерировать секреты

Выполните в PowerShell:

```powershell
# ENCRYPTION_SECRET (минимум 32 символа)
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))

# SESSION_SECRET
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))

# CRON_SECRET
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

---

## 🚀 Деплой на Vercel

### Шаг 1: Environment Variables

В Vercel Dashboard → Settings → Environment Variables добавьте:

```env
# Обязательные
DATABASE_URL=postgresql://...
ENCRYPTION_SECRET=<сгенерированный секрет>
SESSION_SECRET=<сгенерированный секрет>
CRON_SECRET=<сгенерированный секрет>
RESEND_API_KEY=re_...
EMAIL_FROM=FATOS.pro <noreply@yourdomain.com>
NEXT_PUBLIC_BASE_URL=https://yourdomain.com
OAUTH_REDIRECT_BASE_URL=https://yourdomain.com

# Опциональные (если используете OAuth)
GOOGLE_OAUTH_CLIENT_ID=...
GOOGLE_OAUTH_CLIENT_SECRET=...
FACEBOOK_OAUTH_CLIENT_ID=...
FACEBOOK_OAUTH_CLIENT_SECRET=...
```

### Шаг 2: Деплой

```bash
# Закоммитить изменения
git add .
git commit -m "Add advanced authentication"
git push

# Деплой
vercel --prod
```

### Шаг 3: Проверка

После деплоя проверьте:

```bash
# Health check
curl https://yourdomain.com/api/auth/session

# Password reset
curl -X POST https://yourdomain.com/api/auth/password-reset/request \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}'
```

---

## 🧪 Локальное тестирование

### Запустить dev сервер

```cmd
npm run dev
```

Сервер запустится на http://localhost:3000

### Протестировать endpoints

```cmd
test-api-endpoints.bat
```

Или вручную:

```cmd
curl -X POST http://localhost:3000/api/auth/password-reset/request -H "Content-Type: application/json" -d "{\"email\":\"admin@fatos.pro\"}"
```

### Запустить тесты

```cmd
npm test -- --testPathPattern=auth
```

Ожидаемый результат: **51 passed** ✅

---

## 📚 Документация

### Основные файлы
- `DEPLOYMENT_GUIDE.md` - полное руководство по деплою
- `FINAL_SUMMARY.md` - итоговая сводка
- `API_ENDPOINTS_SUMMARY.md` - документация API (EN)
- `КАК_ТЕСТИРОВАТЬ_API.md` - тестирование (RU)

### API Endpoints

**Password Reset:**
- POST `/api/auth/password-reset/request`
- GET `/api/auth/password-reset/verify`
- POST `/api/auth/password-reset/confirm`

**Email Verification:**
- POST `/api/auth/email/verify`
- POST `/api/auth/email/resend`

**2FA:**
- POST `/api/auth/2fa/setup`
- POST `/api/auth/2fa/confirm`
- POST `/api/auth/2fa/verify`
- POST `/api/auth/2fa/disable`
- POST `/api/auth/2fa/backup-codes/regenerate`

**OAuth:**
- GET `/api/auth/oauth/[provider]/authorize`
- GET `/api/auth/oauth/[provider]/callback`
- POST `/api/auth/oauth/link`
- POST `/api/auth/oauth/unlink`

**Admin:**
- GET `/api/admin/auth/stats`
- GET `/api/admin/users`

**User Profile:**
- GET `/api/user/profile`
- PUT `/api/user/profile`

---

## 🔒 Безопасность

### Что реализовано
- ✅ Rate limiting (защита от brute-force)
- ✅ Email enumeration prevention
- ✅ CSRF protection (OAuth)
- ✅ Token hashing (SHA-256)
- ✅ Password hashing (bcrypt)
- ✅ Data encryption (AES-256-GCM)
- ✅ Security logging
- ✅ Session management

### Checklist перед production
- [ ] Все секреты сгенерированы случайно
- [ ] ENCRYPTION_SECRET минимум 32 символа
- [ ] Resend домен верифицирован
- [ ] OAuth redirect URLs настроены
- [ ] Database использует SSL
- [ ] HTTPS включен

---

## 📊 Мониторинг

### Admin Dashboard

Статистика аутентификации:
```
GET /api/admin/auth/stats
```

Список пользователей:
```
GET /api/admin/users?page=1&limit=50
```

### Database Queries

Последние security events:
```sql
SELECT * FROM "SecurityLog" 
ORDER BY "createdAt" DESC 
LIMIT 100;
```

Статистика пользователей:
```sql
SELECT 
  COUNT(*) as total,
  COUNT(*) FILTER (WHERE "emailVerified" = true) as verified,
  COUNT(*) FILTER (WHERE "twoFactorEnabled" = true) as with_2fa
FROM "User";
```

---

## 🐛 Troubleshooting

### Email не отправляются
1. Проверьте `RESEND_API_KEY` в Vercel
2. Убедитесь что домен верифицирован
3. Проверьте логи: `vercel logs --follow`

### OAuth не работает
1. Проверьте redirect URLs в консоли провайдера
2. Убедитесь что `OAUTH_REDIRECT_BASE_URL` правильный
3. Проверьте client ID и secret

### Cron jobs не выполняются
1. Проверьте `vercel.json` задеплоен
2. Убедитесь что `CRON_SECRET` установлен
3. Проверьте Vercel dashboard → Cron Jobs

---

## ✅ Готово!

Система полностью готова к использованию. Следуйте инструкциям выше для деплоя.

**Вопросы?** Смотрите `DEPLOYMENT_GUIDE.md` для подробных инструкций.

---

**Дата:** 22.02.2026  
**Статус:** ✅ Production Ready
