# Чеклист деплоя на Vercel

## ✅ Перед деплоем

### 1. Проверка кода
- ✅ Все файлы сохранены
- ✅ Prisma schema обновлена
- ✅ package.json обновлен с новыми зависимостями
- ✅ .env.example обновлен

### 2. Git коммит
```bash
git add .
git commit -m "feat: add advanced authentication services (35% complete)"
git push
```

### 3. Переменные окружения в Vercel

Добавьте в Vercel Dashboard → Settings → Environment Variables:

**Обязательные:**
```
DATABASE_URL=postgresql://...  (ваш Neon URL)
ENCRYPTION_SECRET=<сгенерируйте случайную строку 32+ символов>
```

**Для email (если хотите тестировать):**
```
RESEND_API_KEY=re_...  (получите на resend.com)
EMAIL_FROM=FATOS.pro <noreply@fatos.pro>
```

**Для OAuth (опционально, пока не реализовано):**
```
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
FACEBOOK_OAUTH_CLIENT_ID=
FACEBOOK_OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_BASE_URL=https://ваш-домен.vercel.app
```

**Другие:**
```
NEXT_PUBLIC_BASE_URL=https://ваш-домен.vercel.app
```

### 4. База данных Neon

✅ SQL миграция уже выполнена в Neon
✅ Все таблицы созданы

---

## 🚀 Деплой

### Вариант 1: Через Vercel Dashboard
1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект
3. Нажмите "Deploy"
4. Дождитесь завершения

### Вариант 2: Через Git
1. Сделайте push в GitHub
2. Vercel автоматически задеплоит

### Вариант 3: Через CLI
```bash
npm install -g vercel
vercel --prod
```

---

## ⚠️ Важно

### Что работает после деплоя:
- ✅ Существующая функциональность (калькулятор, премиум услуги, админка)
- ✅ Новые сервисы (но без API endpoints пока не используются)
- ✅ База данных с новыми таблицами

### Что НЕ работает (еще не реализовано):
- ❌ Password reset (нет API endpoints)
- ❌ Email verification (нет API endpoints)
- ❌ 2FA (нет API endpoints)
- ❌ OAuth (нет сервиса и API endpoints)

### Новый код не сломает существующую функциональность:
- Новые сервисы не используются пока
- Новые таблицы не влияют на старый код
- Все изменения обратно совместимы

---

## 🔍 После деплоя

### Проверьте:
1. Сайт открывается
2. Существующие функции работают
3. Нет ошибок в логах Vercel

### Если есть ошибки:
1. Проверьте Vercel Logs
2. Убедитесь что все env переменные установлены
3. Проверьте что DATABASE_URL правильный

---

## 📝 Команды для деплоя

```bash
# 1. Проверить что все работает локально
npm run build

# 2. Закоммитить изменения
git add .
git status
git commit -m "feat: add advanced authentication services (database, encryption, tokens, email, 2FA)"

# 3. Запушить в GitHub
git push origin main

# 4. Vercel автоматически задеплоит
# Или вручную через dashboard
```

---

## 🎯 Следующий деплой

После создания API endpoints (следующая сессия):
- Password reset будет работать
- Email verification будет работать
- 2FA будет работать
- OAuth будет работать (если добавим)

---

**Готово к деплою:** ✅ ДА
**Безопасно:** ✅ ДА (не сломает существующий функционал)
**Прогресс:** 35% расширенной аутентификации
