# Инструкция по обновлению описаний арканов

## ✅ Готово!

SQL скрипт с вашими описаниями успешно создан: **`prisma/insert-arcana-descriptions-full.sql`**

## Что содержит скрипт

- **22 аркана** (1-22)
- **44 записи** (22 аркана × 2 языка: русский и английский)
- Каждый аркан содержит описания для:
  - 🌅 Утро
  - ☀️ День
  - 🌇 Вечер
  - 🌙 Ночь

## Как использовать

### Шаг 1: Удалите старые статьи (опционально)

Откройте Neon SQL Editor и выполните:

```sql
-- Проверьте, что будет удалено
SELECT id, title, "relatedValue", language
FROM "Article" 
WHERE category = 'Arcana (Cards)'
ORDER BY "relatedValue", language;

-- Удалите старые статьи
DELETE FROM "Article" WHERE category = 'Arcana (Cards)';

-- Проверьте результат
SELECT COUNT(*) FROM "Article" WHERE category = 'Arcana (Cards)';
-- Должно быть 0
```

### Шаг 2: Добавьте новые статьи

1. Откройте файл `prisma/insert-arcana-descriptions-full.sql`
2. Скопируйте весь SQL код
3. Вставьте в Neon SQL Editor
4. Нажмите "Run"

### Шаг 3: Проверьте результат

```sql
-- Должно быть 44 записи
SELECT COUNT(*) as "Всего статей" 
FROM "Article" 
WHERE category = 'Arcana (Cards)';

-- Проверьте структуру
SELECT "relatedValue", language, title 
FROM "Article" 
WHERE category = 'Arcana (Cards)' 
ORDER BY "relatedValue", language;
```

### Шаг 4: Проверьте в приложении

1. Откройте http://localhost:3001/ru
2. Выполните расчет арканов
3. Проверьте, что описания отображаются корректно
4. Проверьте на обоих языках (ru и en)

## Структура описания

Каждый аркан содержит:

```
🌅 УТРО
[Описание для утра]

☀️ ДЕНЬ
[Описание для дня]

🌇 ВЕЧЕР
[Описание для вечера]

🌙 НОЧЬ
[Описание для ночи]
```

## Важные замечания

### Нумерация арканов

- В вашем JSON: арканы 1-22
- В базе данных: `arcana_1` до `arcana_22`
- В расчетах: используются арканы 1-22

**Аркан 22 = Шут** (используется в расчетах, когда результат = 22)

### Английские переводы

⚠️ **ВНИМАНИЕ:** В текущем скрипте английские версии содержат те же тексты, что и русские.

Вам нужно будет:
1. Перевести все описания на английский
2. Обновить скрипт `generate-arcana-sql.js`
3. Перегенерировать SQL файл

Или вы можете обновить английские версии вручную через админ-панель или SQL.

## Обновление английских переводов

### Вариант 1: Через SQL

```sql
-- Пример обновления английской версии аркана 1
UPDATE "Article"
SET content = E'🌅 MORNING\n[English morning description]\n\n☀️ DAY\n[English day description]\n\n🌇 EVENING\n[English evening description]\n\n🌙 NIGHT\n[English night description]',
    "updatedAt" = NOW()
WHERE "relatedValue" = 'arcana_1' AND language = 'en';
```

### Вариант 2: Через админ-панель

1. Войдите в админ-панель: http://localhost:3001/ru/admin
2. Найдите статьи с category = "Arcana (Cards)" и language = "en"
3. Отредактируйте каждую статью

### Вариант 3: Обновите JSON и перегенерируйте

1. Добавьте английские переводы в JSON
2. Обновите скрипт `generate-arcana-sql.js`
3. Запустите: `node generate-arcana-sql.js`
4. Выполните новый SQL файл

## Проверка работы

### Тест 1: Проверка загрузки статей

```bash
# Откройте DevTools → Network
# Выполните расчет арканов
# Должны быть запросы:
# GET /api/articles?relatedValue=arcana_X&language=ru
```

### Тест 2: Проверка отображения

1. Выполните расчет с данными из примера:
   - Дата рождения: 13.12.1984
   - Имя: Алексей
   - Текущая дата: 20.02.2026

2. Ожидаемые арканы:
   - Утро: 16 (Башня)
   - День: 11 (Справедливость)
   - Вечер: 10 (Колесо Фортуны)
   - Ночь: 15 (Дьявол)

3. Проверьте, что для каждого аркана отображается описание с разделами:
   - 🌅 УТРО
   - ☀️ ДЕНЬ
   - 🌇 ВЕЧЕР
   - 🌙 НОЧЬ

## Откат изменений

Если нужно вернуть старые статьи:

```sql
-- Удалите новые статьи
DELETE FROM "Article" WHERE category = 'Arcana (Cards)';

-- Выполните старый скрипт
-- prisma/seed-arcana-articles.sql
```

## Дополнительные файлы

- `generate-arcana-sql.js` - Node.js скрипт для генерации SQL
- `generate-arcana-sql.py` - Python скрипт (альтернатива)
- `prisma/insert-arcana-full.sql` - Частично созданный файл (можно удалить)
- `prisma/delete-old-arcana-articles.sql` - Скрипт для удаления старых статей

## Следующие шаги

1. ✅ Выполните SQL скрипт в Neon
2. ✅ Проверьте в приложении
3. ⚠️ Добавьте английские переводы
4. ✅ Протестируйте на production

Готово! Ваши описания арканов теперь в базе данных. 🎉
