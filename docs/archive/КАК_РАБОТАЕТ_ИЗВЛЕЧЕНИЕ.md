# Как работает извлечение описаний по времени суток

## Схема работы

### 1. Полная статья в базе данных
```
🌅 УТРО
Утро силы. Ты чувствуешь, что можешь всё...

☀️ ДЕНЬ
День реализации. Ты — центр событий...

🌇 ВЕЧЕР
Вечером важно закрепить результат...

🌙 НОЧЬ
Ночь удовлетворения. Ты засыпаешь с чувством...
```

### 2. Извлечение для каждой карты

#### Карта "Утро" → извлекает только:
```
Утро силы. Ты чувствуешь, что можешь всё...
```

#### Карта "День" → извлекает только:
```
День реализации. Ты — центр событий...
```

#### Карта "Вечер" → извлекает только:
```
Вечером важно закрепить результат...
```

#### Карта "Ночь" → извлекает только:
```
Ночь удовлетворения. Ты засыпаешь с чувством...
```

## Алгоритм извлечения

```javascript
function extractTimeOfDayContent(fullContent, timeOfDay) {
  // 1. Определяем паттерны для поиска
  const patterns = {
    morning: '🌅 УТРО',
    day: '☀️ ДЕНЬ',
    evening: '🌇 ВЕЧЕР',
    night: '🌙 НОЧЬ'
  };
  
  // 2. Находим начало нужного раздела
  const currentPattern = patterns[timeOfDay];
  const startIndex = fullContent.indexOf(currentPattern);
  
  // 3. Находим начало следующего раздела
  let endIndex = fullContent.length;
  for (const pattern of Object.values(patterns)) {
    if (pattern === currentPattern) continue;
    const nextIndex = fullContent.indexOf(pattern, startIndex + 1);
    if (nextIndex !== -1 && nextIndex < endIndex) {
      endIndex = nextIndex;
    }
  }
  
  // 4. Извлекаем текст между началом и концом
  let extracted = fullContent.substring(startIndex, endIndex);
  
  // 5. Убираем заголовок раздела
  extracted = extracted.replace(currentPattern, '').trim();
  
  return extracted;
}
```

## Пример работы

### Входные данные:
- **fullContent**: полная статья со всеми 4 описаниями
- **timeOfDay**: 'morning' (для карты "Утро")

### Шаги:
1. **Ищем** "🌅 УТРО" → находим на позиции 0
2. **Ищем** следующий заголовок "☀️ ДЕНЬ" → находим на позиции 115
3. **Извлекаем** текст от 0 до 115
4. **Убираем** заголовок "🌅 УТРО"
5. **Возвращаем**: "Утро силы. Ты чувствуешь, что можешь всё..."

### Результат:
```
Утро силы. Ты чувствуешь, что можешь всё. 
Сегодня твоя воля и слово имеют вес. 
Начинай важные дела именно сейчас.
```

## Преимущества подхода

✅ **Одна статья в БД** - не нужно создавать 4 отдельные статьи для каждого времени суток
✅ **Гибкость** - легко изменить описания, отредактировав одну статью
✅ **Производительность** - один запрос к БД вместо четырех
✅ **Простота** - извлечение происходит на клиенте, без дополнительной логики на сервере

## Поддержка языков

Функция автоматически определяет язык и использует правильные паттерны:

### Русский (ru):
- 🌅 УТРО
- ☀️ ДЕНЬ
- 🌇 ВЕЧЕР
- 🌙 НОЧЬ

### Английский (en):
- 🌅 MORNING
- ☀️ DAY
- 🌇 EVENING
- 🌙 NIGHT

## Тестирование

Запустите тест для проверки:
```bash
node test-content-extraction.js
```

Тест покажет:
- Полный контент статьи
- Извлеченный текст для каждого времени суток
- Длину каждого извлеченного фрагмента
