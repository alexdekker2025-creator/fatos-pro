# 📋 Пошаговая инструкция: Добавление описаний арканов

## ✅ ШАГ 1: Откройте Neon Dashboard

1. Перейдите на сайт: https://console.neon.tech/
2. Войдите в свой аккаунт
3. Выберите проект **fatos-pro** (или как называется ваш проект)
4. Нажмите на кнопку **"SQL Editor"** в левом меню

---

## ✅ ШАГ 2: Удалите старые статьи арканов

### В SQL Editor выполните:

```sql
-- Сначала посмотрите, что будет удалено
SELECT id, title, "relatedValue", language
FROM "Article" 
WHERE category = 'Arcana (Cards)'
ORDER BY "relatedValue", language;
```

**Нажмите "Run"** (или Ctrl+Enter)

Вы увидите список всех текущих статей арканов (должно быть 44 записи).

### Теперь удалите их:

```sql
DELETE FROM "Article" WHERE category = 'Arcana (Cards)';
```

**Нажмите "Run"**

### Проверьте, что удалилось:

```sql
SELECT COUNT(*) FROM "Article" WHERE category = 'Arcana (Cards)';
```

**Результат должен быть: 0**

---

## ✅ ШАГ 3: Откройте файл с новыми описаниями

### На вашем компьютере:

1. Откройте папку проекта: `C:\Users\user\Desktop\fatos.pro`
2. Откройте папку `prisma`
3. Найдите файл: **`insert-arcana-descriptions-full.sql`**
4. Откройте его в любом текстовом редакторе (Notepad, VS Code, и т.д.)

---

## ✅ ШАГ 4: Скопируйте SQL код

1. В открытом файле `insert-arcana-descriptions-full.sql`
2. Нажмите **Ctrl+A** (выделить всё)
3. Нажмите **Ctrl+C** (скопировать)

---

## ✅ ШАГ 5: Вставьте код в Neon SQL Editor

1. Вернитесь в Neon SQL Editor (в браузере)
2. Очистите поле ввода (удалите предыдущие запросы)
3. Нажмите **Ctrl+V** (вставить скопированный код)
4. Нажмите **"Run"** (или Ctrl+Enter)

⏳ **Подождите 5-10 секунд** - скрипт добавляет 44 записи

---

## ✅ ШАГ 6: Проверьте результат

### В SQL Editor выполните:

```sql
-- Должно быть 44 записи
SELECT COUNT(*) as "Всего статей" 
FROM "Article" 
WHERE category = 'Arcana (Cards)';
```

**Результат должен быть: 44**

### Проверьте структуру:

```sql
SELECT "relatedValue", language, title 
FROM "Article" 
WHERE category = 'Arcana (Cards)' 
ORDER BY "relatedValue", language
LIMIT 10;
```

Вы должны увидеть:
- arcana_1, ru, Маг
- arcana_1, en, The Magician
- arcana_2, ru, Верховная Жрица
- arcana_2, en, The High Priestess
- и т.д.

---

## ✅ ШАГ 7: Проверьте в приложении

### Откройте браузер:

1. Перейдите на: http://localhost:3001/ru
2. Если сервер не запущен, запустите его:
   ```bash
   npm run dev
   ```

### Выполните расчет:

1. Введите данные:
   - **Имя:** Алексей
   - **Дата рождения:** 13.12.1984
   - **Текущая дата:** (оставьте сегодняшнюю)

2. Нажмите **"Рассчитать"**

3. Вы должны увидеть 4 аркана с описаниями

### Проверьте описание аркана:

1. Нажмите на любую карту аркана (если есть flip-анимация)
2. Или просто посмотрите на отображаемый текст

Вы должны увидеть структуру:
```
🌅 УТРО
[Описание для утра]

☀️ ДЕНЬ
[Описание для дня]

🌇 ВЕЧЕР
[Описание для вечера]

🌙 НОЧЬ
[Описание для ночи]
```

---

## ✅ ШАГ 8: Проверьте на английском языке

1. Переключите язык на английский (если есть переключатель)
2. Или откройте: http://localhost:3001/en
3. Выполните расчет снова
4. Проверьте, что описания отображаются

⚠️ **ВАЖНО:** Сейчас английские версии содержат те же тексты, что и русские. Это нормально - переводы можно добавить позже.

---

## ✅ ШАГ 9: Проверьте в DevTools (опционально)

### Откройте DevTools:

1. Нажмите **F12** в браузере
2. Перейдите на вкладку **"Network"**
3. Выполните расчет арканов
4. Найдите запросы типа:
   ```
   GET /api/articles?relatedValue=arcana_16&language=ru
   GET /api/articles?relatedValue=arcana_11&language=ru
   GET /api/articles?relatedValue=arcana_10&language=ru
   GET /api/articles?relatedValue=arcana_15&language=ru
   ```

5. Нажмите на любой запрос
6. Посмотрите **"Response"** - там должен быть JSON с вашими описаниями

---

## ✅ ШАГ 10: Деплой на production (когда будете готовы)

### Когда всё работает локально:

1. Откройте Neon Dashboard для **production** базы данных
2. Повторите **ШАГ 2-6** для production базы
3. Проверьте на: https://fatos-pro.vercel.app/ru

---

## 🎉 ГОТОВО!

Ваши описания арканов теперь в базе данных и отображаются в приложении!

---

## 📝 Что дальше?

### Если нужно добавить английские переводы:

**Вариант 1: Через SQL**

```sql
-- Пример для аркана 1
UPDATE "Article"
SET content = E'🌅 MORNING\n[English morning text]\n\n☀️ DAY\n[English day text]\n\n🌇 EVENING\n[English evening text]\n\n🌙 NIGHT\n[English night text]',
    "updatedAt" = NOW()
WHERE "relatedValue" = 'arcana_1' AND language = 'en';
```

**Вариант 2: Через админ-панель**

1. Откройте: http://localhost:3001/ru/admin
2. Войдите как администратор
3. Найдите статьи с category = "Arcana (Cards)"
4. Отредактируйте английские версии

**Вариант 3: Обновите JSON и перегенерируйте**

1. Добавьте английские тексты в JSON
2. Обновите файл `generate-arcana-sql.js`
3. Запустите: `node generate-arcana-sql.js`
4. Повторите ШАГ 3-6

---

## ❓ Если что-то пошло не так

### Проблема: "Ошибка при выполнении SQL"

**Решение:**
- Проверьте, что вы скопировали весь код из файла
- Убедитесь, что в SQL Editor нет лишних символов
- Попробуйте выполнить скрипт по частям (по 5-10 арканов)

### Проблема: "Статьи не отображаются в приложении"

**Решение:**
1. Проверьте в DevTools → Network, что запросы к `/api/articles` выполняются
2. Проверьте в Neon, что записи действительно добавлены
3. Перезапустите dev-сервер: `npm run dev`
4. Очистите кэш браузера (Ctrl+Shift+R)

### Проблема: "Показывает 'Загрузка описания...'"

**Решение:**
- Это значит, что статья не найдена в базе
- Проверьте `relatedValue` в базе данных
- Убедитесь, что формат: `arcana_1`, `arcana_2`, и т.д. (без пробелов)

---

## 📞 Контакты

Если возникнут вопросы:
1. Проверьте логи в DevTools Console
2. Проверьте логи в Neon Dashboard
3. Проверьте файл `ARCANA_UPDATE_INSTRUCTIONS.md` для дополнительной информации

---

**Удачи! 🚀**
