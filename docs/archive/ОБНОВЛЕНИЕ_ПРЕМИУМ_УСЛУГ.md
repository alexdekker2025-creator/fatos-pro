# Обновление премиум услуг - Инструкция

## ✅ Что уже сделано

1. **SQL скрипт создан**: `prisma/update-premium-services.sql`
   - Удаляет старые данные услуг
   - Вставляет 7 обновлённых услуг с новыми описаниями
   - Все услуги имеют тарифы СТАРТ и ГЛУБОКИЙ

2. **Fallback данные обновлены**: `components/PremiumServices.tsx`
   - Обновлены hookRu/hookEn для всех услуг
   - Обновлены features для тарифов ГЛУБОКИЙ
   - Компонент будет показывать новые данные даже если база недоступна
   - **Добавлена защита от ошибок**: проверка на undefined перед вызовом .map()

3. **Скрипт проверки создан**: `check-premium-services.js`
   - Проверяет количество услуг в базе
   - Проверяет корректность цен
   - Проверяет наличие описаний (hook)

## 📋 Что нужно сделать

### Шаг 1: Выполнить SQL миграцию

Откройте Neon SQL Editor и выполните скрипт:

```bash
# Путь к файлу
prisma/update-premium-services.sql
```

**Что делает скрипт:**
- Удаляет все старые записи из таблицы PremiumService
- Вставляет 7 новых услуг с обновлёнными описаниями
- Проверяет количество активных услуг (должно быть 7)

### Шаг 2: Проверить результат

После выполнения SQL скрипта проверьте с помощью Node.js скрипта:

```bash
node check-premium-services.js
```

Или проверьте вручную в SQL:

```sql
SELECT "serviceId", "titleRu", "priceBasicRUB", "priceFullRUB" 
FROM "PremiumService" 
WHERE "isActive" = true 
ORDER BY "sortOrder";
```

**Ожидаемый результат:**
```
serviceId           | titleRu              | priceBasicRUB | priceFullRUB
--------------------|----------------------|---------------|-------------
destiny_matrix      | Матрица судьбы       | 3500          | 5500
child_numerology    | Детская нумерология  | 2900          | 4900
compatibility       | Совместимость        | 3900          | 5900
money_numerology    | Денежная нумерология | 3900          | 5900
yearly_forecast     | Годовой прогноз      | 4900          | 6900
pythagorean_full    | Квадрат Пифагора     | 2900          | 4900
pro_access          | Профи-доступ         | 500           | 2500
```

### Шаг 3: Проверить на сайте

1. Откройте сайт: http://localhost:3001 или https://fatos-pro.vercel.app/ru
2. Перейдите в раздел "Премиум услуги"
3. Проверьте, что:
   - Отображаются все 7 услуг
   - Каждая услуга имеет два тарифа: СТАРТ (🌿) и ГЛУБОКИЙ (🔥)
   - Цены соответствуют новым значениям
   - Описания обновлены (новые hookRu/hookEn)

### Шаг 4: Проверить на английском языке

1. Переключите язык на английский
2. Убедитесь, что все тексты переведены
3. Цены должны отображаться в обеих валютах (₽ и $)

## 🔍 Что изменилось

### Новые описания (hookRu/hookEn)

| Услуга | Старое описание | Новое описание |
|--------|----------------|----------------|
| Матрица судьбы | Базовый расчёт матрицы, 4 ключевых аркана... | Для тех, кто хочет заглянуть в себя |
| Детская нумерология | Расшифровка основных черт характера... | Для родителей, которые хотят быть ближе |
| Совместимость | Общий вывод о совместимости... | Для тех, кто хочет проверить себя и партнёра |
| Денежная нумерология | 4 цифры вашего денежного кода... | Для тех, кто хочет узнать свой финансовый потенциал |
| Годовой прогноз | Помесячный прогноз и общая энергия года | Для тех, кто хочет заглянуть в будущее |
| Квадрат Пифагора | Готовая таблица 3×3 с цифрами... | Для тех, кто хочет понять себя через цифры |
| Профи-доступ | Сухие профессиональные данные... | (без изменений) |

### Обновлённые features для тарифа ГЛУБОКИЙ

**Совместимость:**
- Было: "Детальный анализ по 5 сферам", "Энергия, интеллект, быт, эмоции, близость" (2 отдельных пункта)
- Стало: "Детальный анализ по 5 сферам: энергия, интеллект, быт, эмоции, близость" (1 пункт)

**Годовой прогноз:**
- Было: "Детальный разбор каждого месяца", "Акцент на деньги, отношения и карьеру" (2 отдельных пункта)
- Стало: "Детальный разбор каждого месяца с акцентом на деньги, отношения и карьеру" (1 пункт)

## ⚠️ Важные замечания

1. **Профи-доступ**: Текущая структура поддерживает только 2 тарифа (разовый 500₽ и месяц 2500₽). Годовая подписка (20000₽) требует расширения схемы базы данных.

2. **Fallback данные**: Обновлены в компоненте, поэтому даже если база данных недоступна, пользователи увидят новые описания.

3. **Локализация**: Все тексты имеют переводы на английский язык.

## 🐛 Если что-то не работает

### Проблема: "Cannot read properties of undefined (reading 'map')"

**Решение:**
✅ Уже исправлено! Добавлена защита от undefined в компоненте:
- Проверка `data.services` перед вызовом `.map()`
- Проверка `service.features?.basic` перед отображением
- Fallback на defaultServices при любых ошибках
- Сообщение "Услуги временно недоступны" если массив пустой

Если ошибка всё ещё возникает:
1. Очистите кэш браузера (Ctrl+Shift+R)
2. Перезапустите dev сервер
3. Проверьте консоль браузера на другие ошибки

### Проблема: Не видны изменения на сайте

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+R)
2. Проверьте, что SQL скрипт выполнен успешно
3. Проверьте консоль браузера на ошибки
4. Проверьте, что API endpoint /api/services возвращает данные

### Проблема: API возвращает пустой массив

**Решение:**
1. Проверьте подключение к базе данных
2. Убедитесь, что SQL скрипт выполнен
3. Проверьте, что поле isActive = true для всех услуг

### Проблема: Отображаются старые данные

**Решение:**
1. Компонент использует fallback данные (они уже обновлены)
2. Выполните SQL миграцию для обновления базы данных
3. После миграции данные будут загружаться из базы

## 📝 Следующие шаги

После успешного обновления данных можно:
1. Добавить тесты для проверки корректности данных
2. Реализовать третий тариф для Профи-доступ (годовая подписка)
3. Добавить поле для целевой аудитории (если требуется)

---

**Статус**: ✅ Готово к выполнению SQL миграции
**Дата**: 2025
**Автор**: Kiro AI Assistant
