# Настройка мониторинга, аналитики и бэкапов для FATOS.pro

## 1. Sentry - Мониторинг ошибок

### Шаг 1: Создание проекта на Sentry

1. Зайдите на https://sentry.io и создайте аккаунт (бесплатно до 5000 событий/месяц)
2. Создайте новый проект:
   - Выберите платформу: **Next.js**
   - Название проекта: **fatos-pro**
3. Скопируйте DSN (Data Source Name) - он будет выглядеть так:
   ```
   https://xxxxxxxxxxxxx@o123456.ingest.sentry.io/123456
   ```

### Шаг 2: Добавление переменных окружения

Добавьте в `.env.local` (локально) и в Vercel (Settings → Environment Variables):

```env
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here
```

Для получения Auth Token:
1. Зайдите в Sentry → Settings → Account → API → Auth Tokens
2. Создайте новый токен с правами: `project:read`, `project:releases`, `org:read`

### Шаг 3: Конфигурация уже создана

Файлы конфигурации Sentry уже созданы:
- `sentry.client.config.ts` - для клиентской части
- `sentry.server.config.ts` - для серверной части
- `sentry.edge.config.ts` - для Edge Runtime
- `next.config.js` - обновлён с Sentry плагином

### Шаг 4: Деплой

После добавления переменных в Vercel, просто сделайте push:
```bash
git push origin main
```

Sentry автоматически начнёт отслеживать ошибки!

---

## 2. Google Analytics - Аналитика

### Шаг 1: Создание Google Analytics аккаунта

1. Зайдите на https://analytics.google.com
2. Создайте аккаунт и ресурс (Property)
3. Выберите **Web** и введите URL: `https://fatos.pro`
4. Скопируйте Measurement ID (формат: `G-XXXXXXXXXX`)

### Шаг 2: Добавление переменной окружения

Добавьте в `.env.local` и Vercel:

```env
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
```

### Шаг 3: Компонент уже создан

Компонент Google Analytics уже создан в `components/GoogleAnalytics.tsx`

### Шаг 4: Деплой

```bash
git push origin main
```

Google Analytics начнёт собирать данные о посетителях!

---

## 3. Автоматические бэкапы базы данных Neon

### Вариант A: Встроенные бэкапы Neon (Рекомендуется)

Neon автоматически создаёт point-in-time recovery бэкапы:

1. Зайдите в https://console.neon.tech
2. Выберите ваш проект **fatos-pro**
3. Перейдите в **Settings → Backups**
4. Включите **Point-in-time restore** (доступно на платных планах от $19/мес)

**Бесплатный план:**
- Автоматические снапшоты каждые 24 часа
- Хранятся 7 дней
- Восстановление через консоль Neon

### Вариант B: Ручные бэкапы через скрипт

Создан скрипт `scripts/backup-database.js` для ручных бэкапов:

```bash
# Локальный бэкап
node scripts/backup-database.js

# Бэкап сохранится в backups/backup-YYYY-MM-DD-HH-mm-ss.sql
```

### Вариант C: Автоматические бэкапы через GitHub Actions

Создан workflow `.github/workflows/database-backup.yml`:

**Настройка:**

1. Добавьте секреты в GitHub:
   - Settings → Secrets and variables → Actions → New repository secret
   - Добавьте: `DATABASE_URL` (ваша строка подключения к Neon)

2. Workflow будет запускаться:
   - Каждый день в 03:00 UTC
   - Вручную через Actions → Database Backup → Run workflow

3. Бэкапы сохраняются как артефакты GitHub (доступны 90 дней)

### Вариант D: Бэкапы в облако (AWS S3 / Google Cloud Storage)

Если нужно хранить бэкапы дольше, можно настроить загрузку в S3:

1. Создайте S3 bucket на AWS
2. Добавьте секреты в GitHub:
   - `AWS_ACCESS_KEY_ID`
   - `AWS_SECRET_ACCESS_KEY`
   - `AWS_S3_BUCKET`
3. Раскомментируйте секцию S3 в `database-backup.yml`

---

## 4. Проверка работы

### Sentry
1. Откройте https://fatos.pro
2. Вызовите тестовую ошибку (если добавили тестовый эндпоинт)
3. Проверьте в Sentry → Issues

### Google Analytics
1. Откройте https://fatos.pro
2. Подождите 5-10 минут
3. Проверьте в Google Analytics → Reports → Realtime

### Бэкапы
```bash
# Проверка ручного бэкапа
node scripts/backup-database.js

# Проверка автоматического бэкапа
# GitHub → Actions → Database Backup → Run workflow
```

---

## 5. Мониторинг расходов

### Бесплатные лимиты:

**Sentry:**
- 5,000 событий/месяц - бесплатно
- Далее: $26/месяц за 50,000 событий

**Google Analytics:**
- Полностью бесплатно
- Лимит: 10 млн событий/месяц

**Neon Backups:**
- Бесплатный план: снапшоты 7 дней
- Scale план ($19/мес): point-in-time restore

**GitHub Actions:**
- 2,000 минут/месяц - бесплатно
- Бэкап занимает ~1-2 минуты

---

## 6. Дополнительные рекомендации

### Uptime мониторинг (опционально)

Можно добавить мониторинг доступности сайта:

**UptimeRobot** (бесплатно):
1. https://uptimerobot.com
2. Добавьте монитор для https://fatos.pro
3. Проверка каждые 5 минут
4. Email уведомления при падении

**Better Uptime** (бесплатно):
1. https://betteruptime.com
2. Более продвинутый мониторинг
3. Интеграция с Slack/Telegram

### Performance мониторинг

Vercel уже предоставляет:
- Analytics → Performance
- Web Vitals (Core Web Vitals)
- Функции и API метрики

---

## Готово! 🎉

Теперь у вас настроены:
- ✅ Мониторинг ошибок (Sentry)
- ✅ Аналитика посетителей (Google Analytics)
- ✅ Автоматические бэкапы БД (Neon + GitHub Actions)

Все инструменты работают в фоновом режиме и не требуют постоянного внимания.
