# Быстрый старт: База данных

Это краткое руководство поможет вам быстро настроить базу данных для FATOS.pro.

## Вариант 1: Локальная разработка (PostgreSQL)

### Шаг 1: Установите PostgreSQL

**Windows:**
- Скачайте с https://www.postgresql.org/download/windows/
- Установите с настройками по умолчанию
- Запомните пароль для пользователя `postgres`

**macOS:**
```bash
brew install postgresql@15
brew services start postgresql@15
```

**Linux:**
```bash
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

### Шаг 2: Создайте базу данных

```bash
# Windows (в командной строке)
psql -U postgres
CREATE DATABASE fatos_pro;
\q

# macOS/Linux
createdb fatos_pro
```

### Шаг 3: Настройте .env

Файл `.env` уже создан. Проверьте, что DATABASE_URL правильный:

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fatos_pro?schema=public"
```

Замените `postgres:postgres` на ваши учётные данные, если они отличаются.

### Шаг 4: Примените миграции

```bash
npm run db:migrate
```

Введите имя миграции, например: `init`

### Шаг 5: Проверьте результат

```bash
npm run db:studio
```

Откроется браузер с Prisma Studio, где вы увидите все таблицы.

---

## Вариант 2: Облачная база данных (Рекомендуется)

### Supabase (Бесплатно)

1. Зарегистрируйтесь на https://supabase.com
2. Создайте новый проект
3. Перейдите в Settings → Database
4. Скопируйте "Connection string" (URI)
5. Вставьте в `.env`:

```env
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"
```

6. Примените миграции:

```bash
npm run db:migrate:deploy
```

### Neon (Бесплатно)

1. Зарегистрируйтесь на https://neon.tech
2. Создайте новый проект
3. Скопируйте Connection String
4. Вставьте в `.env`:

```env
DATABASE_URL="postgresql://[USER]:[PASSWORD]@[HOST]/[DATABASE]?sslmode=require"
```

5. Примените миграции:

```bash
npm run db:migrate:deploy
```

---

## Полезные команды

```bash
# Создать и применить миграцию
npm run db:migrate

# Применить pending миграции (production)
npm run db:migrate:deploy

# Сгенерировать Prisma Client
npm run db:generate

# Открыть Prisma Studio (GUI для БД)
npm run db:studio

# Проверить статус миграций
npm run db:status

# Сбросить БД (УДАЛИТ ВСЕ ДАННЫЕ!)
npm run db:reset

# Синхронизировать схему без миграций (dev only)
npm run db:push
```

---

## Использование скриптов

### Windows (PowerShell)

```powershell
.\scripts\migrate.ps1
```

### macOS/Linux (Bash)

```bash
chmod +x scripts/migrate.sh
./scripts/migrate.sh
```

Скрипты предоставляют интерактивное меню для всех операций с БД.

---

## Troubleshooting

### "Can't reach database server"

**Проблема:** Не удаётся подключиться к БД.

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте DATABASE_URL в `.env`
3. Проверьте логин/пароль
4. Для облачных БД проверьте интернет-соединение

### "Migration failed"

**Проблема:** Миграция не применилась.

**Решение:**
1. Проверьте логи ошибок
2. Убедитесь, что БД пустая (для первой миграции)
3. Попробуйте `npm run db:reset` (удалит данные!)

### "Prisma Client not found"

**Проблема:** Prisma Client не сгенерирован.

**Решение:**
```bash
npm run db:generate
```

---

## Следующие шаги

После успешного применения миграций:

1. ✅ База данных готова
2. 📝 Можно начинать разработку API endpoints
3. 🧪 Можно писать тесты с реальной БД
4. 🚀 Можно деплоить на Netlify

Подробная документация: `docs/database-migrations.md`
