# Документ требований: Завершение системы арканов

## Введение

Данный документ описывает требования для завершения системы арканов на сайте нумерологии fatos.pro. Система уже включает алгоритм расчета 4 арканов дня (Утро, День, Вечер, Ночь), но требует доработки в части автоматического обновления, сохранения данных пользователя и загрузки контента статей.

**КРИТИЧЕСКИ ВАЖНО:** Текущий алгоритм расчета арканов НЕВЕРЕН и должен быть заменен на корректный алгоритм, описанный ниже.

## Корректный алгоритм расчета 4 арканов

### Входные данные:
- **Дата рождения**: DD.MM.YYYY
- **Текущая дата**: DD.MM.YYYY
- **Имя**: полное имя пользователя (русские или английские буквы)

### Шаг 1: Расчет сумм

**Сумма даты рождения** (с мастер-числами):
1. Суммировать все цифры даты рождения
2. Сводить к однозначному числу путем повторного суммирования цифр
3. **ИСКЛЮЧЕНИЕ**: Если результат 11 или 22, ОСТАНОВИТЬСЯ - это мастер-числа
4. Пример: 13.12.1984 → 1+3+1+2+1+9+8+4 = 29 → 2+9 = 11 (СТОП)

**Сумма текущей даты** (с мастер-числами):
1. Суммировать все цифры текущей даты
2. Сводить к однозначному числу
3. **ИСКЛЮЧЕНИЕ**: Если результат 11 или 22, ОСТАНОВИТЬСЯ
4. Пример: 20.02.2026 → 2+0+0+2+2+0+2+6 = 14 → 1+4 = 5

**Сумма имени** (всегда сводить к однозначному):
1. Преобразовать каждую букву в число по таблице
2. Суммировать все числа
3. Сводить к однозначному числу (нет исключения для мастер-чисел)
4. Пример: Алексей → 1+4+6+3+1+6+2 = 23 → 2+3 = 5

### Шаг 2: Расчет 4 арканов

**🌅 Аркан Утро:**
```
Утро = (Сумма даты рождения + Сумма текущей даты) mod 22
Если результат = 0, то Утро = 22
```

**☀️ Аркан День:**
```
День = (Сумма даты рождения × Сумма текущей даты) mod 22
Если результат = 0, то День = 22
```

**🌇 Аркан Вечер:**
```
Вечер = (Сумма имени + Сумма текущей даты) mod 22
Если результат = 0, то Вечер = 22
```

**🌙 Аркан Ночь:**
```
Ночь = (Утро + День + Вечер) mod 22
Если результат = 0, то Ночь = 22
```

**Правило приведения:**
- Если результат > 22: вычитать 22 повторно, пока не войдет в диапазон 1-22
- Если результат = 0: установить 22
- Валидный диапазон: 1-22 (включительно)

### Ключевые изменения от предыдущего алгоритма:

- **Утро**: было `только дата рождения` → теперь `дата рождения + текущая дата`
- **День**: было `только текущая дата` → теперь `дата рождения × текущая дата`
- **Вечер**: было `имя + утро + день` → теперь `имя + текущая дата`
- **Ночь**: было `день + вечер` → теперь `утро + день + вечер`
- **ВСЕ арканы ТЕПЕРЬ меняются ежедневно** (все зависят от текущей даты)

## Глоссарий

- **Arcana_System** (Система арканов): Подсистема сайта fatos.pro, отвечающая за расчет и отображение 4 арканов дня
- **Calculator** (Калькулятор): Компонент Calculator.tsx, выполняющий расчет нумерологических данных
- **CardOfDay** (Карта дня): Компонент CardOfDay.tsx, отображающий 4 аркана дня
- **User_Profile** (Профиль пользователя): Запись в таблице User базы данных PostgreSQL
- **Arcana_Article** (Статья аркана): Запись в таблице Article с описанием одного из 22 арканов Таро
- **Midnight_Check** (Проверка полуночи): Механизм автоматической проверки времени для обновления арканов
- **Database** (База данных): PostgreSQL база данных, управляемая через Prisma ORM
- **Session** (Сессия): Активная сессия аутентифицированного пользователя

## Требования

### Требование 1: Автоматическое обновление арканов в полночь

**Пользовательская история:** Как пользователь, я хочу, чтобы арканы дня автоматически обновлялись в полночь, чтобы мне не приходилось вручную пересчитывать их каждый день.

#### Критерии приемки

1. THE Arcana_System SHALL проверять текущее время каждую минуту
2. WHEN текущее время достигает 00:00 (полночь), THE Arcana_System SHALL пересчитывать ВСЕ четыре аркана (Утро, День, Вечер, Ночь)
3. **ВАЖНО:** ВСЕ арканы зависят от текущей даты в новом алгоритме и должны пересчитываться ежедневно
4. WHEN пересчет арканов завершен, THE Arcana_System SHALL обновлять отображение CardOfDay без перезагрузки страницы
5. THE Arcana_System SHALL использовать обновленные функции из lib/arcana/arcanaCalculator.ts для пересчета с корректным алгоритмом
6. WHEN пользователь не находится на странице с калькулятором, THE Arcana_System SHALL выполнять пересчет при следующем открытии страницы
7. FOR ALL пересчетов в полночь, ВСЕ арканы SHALL рассчитываться на основе новой текущей даты

### Требование 2: Сохранение имени пользователя в профиле

**Пользовательская история:** Как зарегистрированный пользователь, я хочу, чтобы мое имя сохранялось в профиле, чтобы оно автоматически загружалось при входе на сайт.

#### Критерии приемки

1. WHEN пользователь выполняет расчет арканов, THE Calculator SHALL сохранять введенное имя в поле name таблицы User
2. WHEN пользователь не аутентифицирован, THE Calculator SHALL сохранять имя только в localStorage
3. WHEN аутентифицированный пользователь открывает страницу калькулятора, THE Calculator SHALL загружать имя из User_Profile
4. THE Arcana_System SHALL создать API endpoint PUT /api/user/profile для обновления имени пользователя
5. WHEN запрос к PUT /api/user/profile выполняется, THE Arcana_System SHALL проверять валидность сессии пользователя
6. IF сессия невалидна, THEN THE Arcana_System SHALL возвращать HTTP статус 401 Unauthorized
7. WHEN имя успешно обновлено в базе данных, THE Arcana_System SHALL возвращать HTTP статус 200 с обновленными данными пользователя
8. THE Calculator SHALL синхронизировать имя между localStorage и User_Profile для аутентифицированных пользователей
9. WHEN имя пользователя обновляется в User_Profile, THE Calculator SHALL также обновлять значение в localStorage

### Требование 3: Загрузка статей арканов в базу данных

**Пользовательская история:** Как пользователь, я хочу видеть описания арканов при переворачивании карт, чтобы понимать значение каждого аркана.

#### Критерии приемки

1. THE Arcana_System SHALL выполнить SQL скрипт prisma/seed-arcana-articles.sql в базе данных Neon
2. WHEN скрипт выполнен, THE Database SHALL содержать 44 записи в таблице Article (22 аркана × 2 языка)
3. FOR ALL записей статей арканов, поле relatedValue SHALL иметь формат "arcana_{number}", где number от 1 до 22
4. FOR ALL записей статей арканов, поле category SHALL иметь значение "Arcana (Cards)"
5. FOR ALL записей статей арканов, поле language SHALL иметь значение "ru" или "en"
6. WHEN CardOfDay загружает статью аркана, THE Arcana_System SHALL использовать существующий API endpoint GET /api/articles
7. WHEN запрос к GET /api/articles выполняется с параметром relatedValue=arcana_{number}, THE Arcana_System SHALL возвращать статью на языке, указанном в параметре language
8. WHEN статья аркана загружена, THE CardOfDay SHALL отображать title и content статьи на обратной стороне карты
9. IF статья для аркана не найдена, THEN THE CardOfDay SHALL отображать сообщение "Загрузка описания..." (или "Loading description..." для английского языка)

### Требование 4: Парсер и сериализация данных арканов

**Пользовательская история:** Как разработчик, я хочу иметь надежный механизм сериализации и десериализации данных арканов, чтобы гарантировать корректность данных при сохранении и загрузке.

#### Критерии приемки

1. THE Arcana_System SHALL создать функцию serializeArcanaData для преобразования объекта арканов в JSON строку
2. THE Arcana_System SHALL создать функцию parseArcanaData для преобразования JSON строки в объект арканов
3. WHEN parseArcanaData получает валидную JSON строку с данными арканов, THE Arcana_System SHALL возвращать объект типа DayCardResult
4. WHEN parseArcanaData получает невалидную JSON строку, THE Arcana_System SHALL возвращать объект ошибки с описательным сообщением
5. THE Arcana_System SHALL создать функцию formatArcanaForDisplay для форматирования данных арканов для отображения пользователю
6. FOR ALL валидных объектов арканов, выполнение parseArcanaData(serializeArcanaData(arcana)) SHALL возвращать эквивалентный объект (round-trip свойство)
7. WHEN данные арканов сохраняются в localStorage, THE Arcana_System SHALL использовать serializeArcanaData
8. WHEN данные арканов загружаются из localStorage, THE Arcana_System SHALL использовать parseArcanaData

## Дополнительные замечания

### Технические детали

- Проект использует Next.js 14 с App Router
- База данных: PostgreSQL (Neon) через Prisma ORM
- Аутентификация: сессии хранятся в таблице Session
- Интернационализация: next-intl с поддержкой русского и английского языков
- Существующие компоненты: Calculator.tsx, CardOfDay.tsx
- Существующие библиотеки: lib/arcana/arcanaCalculator.ts

### Зависимости

- Требование 2 зависит от существующей системы аутентификации
- Требование 3 зависит от существующего API endpoint /api/articles
- Требование 1 может быть реализовано независимо от других требований
- Требование 4 должно быть реализовано до или параллельно с Требованием 1

### Ограничения

- Автоматическое обновление арканов работает только когда пользователь находится на странице (client-side)
- Имя пользователя в localStorage используется как fallback для неаутентифицированных пользователей
- SQL скрипт должен быть выполнен вручную администратором базы данных или через миграцию Prisma
