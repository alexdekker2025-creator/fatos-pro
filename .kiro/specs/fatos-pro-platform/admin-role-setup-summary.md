# Настройка роли администратора - Summary

## Дата: 2026-02-21

## Выполнено

### 1. Обновление схемы Prisma ✅
**Файл**: `prisma/schema.prisma`

Добавлено поле `isAdmin` в модель User:

```prisma
model User {
  // ... существующие поля
  isAdmin         Boolean       @default(false)
  // ...
}
```

**Изменения**:
- Добавлено поле `isAdmin` типа Boolean
- Значение по умолчанию: `false`
- Все существующие пользователи получат `isAdmin = false`

---

### 2. SQL миграция ✅
**Файл**: `prisma/migrations/add_is_admin_field.sql`

SQL скрипт для добавления поля в существующую базу данных:

```sql
ALTER TABLE "User" ADD COLUMN "isAdmin" BOOLEAN NOT NULL DEFAULT false;
COMMENT ON COLUMN "User"."isAdmin" IS 'Флаг администратора';
```

**Применение**:
```bash
npx prisma migrate dev --name add_is_admin_field
```

---

### 3. Скрипт назначения администратора ✅
**Файл**: `scripts/make-admin.ts`

TypeScript скрипт для назначения роли администратора пользователю.

**Функциональность**:
- Поиск пользователя по email
- Проверка существования пользователя
- Проверка текущего статуса администратора
- Обновление поля `isAdmin` на `true`
- Вывод информации о пользователе
- Обработка ошибок с понятными сообщениями

**Использование**:
```bash
npx ts-node scripts/make-admin.ts admin@fatos.pro
```

**Вывод при успехе**:
```
🔍 Поиск пользователя с email: admin@fatos.pro...
✅ Успешно! Пользователь назначен администратором:
   ID: clx123abc456
   Имя: Admin User
   Email: admin@fatos.pro
   Администратор: Да

🎉 Теперь пользователь может получить доступ к /admin
```

---

### 4. PowerShell скрипт (Windows) ✅
**Файл**: `scripts/make-admin.ps1`

Обертка для Windows PowerShell.

**Использование**:
```powershell
.\scripts\make-admin.ps1 admin@fatos.pro
```

**Особенности**:
- Цветной вывод
- Проверка кода возврата
- Удобный интерфейс

---

### 5. Bash скрипт (Linux/Mac) ✅
**Файл**: `scripts/make-admin.sh`

Обертка для Linux/Mac.

**Использование**:
```bash
chmod +x scripts/make-admin.sh
./scripts/make-admin.sh admin@fatos.pro
```

**Особенности**:
- Проверка аргументов
- Цветной вывод (emoji)
- Проверка кода возврата

---

### 6. Обновление AdminService ✅
**Файл**: `lib/services/admin/AdminService.ts`

Метод `isAdmin()` теперь использует поле `isAdmin` из базы данных вместо переменной окружения `ADMIN_EMAILS`.

**Было (устаревший метод)**:
```typescript
async isAdmin(userId: string): Promise<boolean> {
  const user = await prisma.user.findUnique({ where: { id: userId } });
  const adminEmails = process.env.ADMIN_EMAILS?.split(',') || [];
  return user ? adminEmails.includes(user.email) : false;
}
```

**Стало (новый метод)**:
```typescript
async isAdmin(userId: string): Promise<boolean> {
  const user = await prisma.user.findUnique({
    where: { id: userId },
    select: { isAdmin: true },
  });
  return user?.isAdmin || false;
}
```

**Преимущества**:
- Более надежно (данные в БД)
- Проще управлять (не нужно редактировать .env)
- Лучше масштабируется
- Можно управлять через UI в будущем

---

### 7. Документация ✅

#### Полная документация
**Файл**: `docs/admin-setup.md`

Подробное руководство включает:
- Предварительные требования
- Пошаговые инструкции
- Применение миграции (3 способа)
- Назначение роли администратора (3 способа)
- Проверка доступа
- Примеры использования
- Снятие роли администратора
- Множественные администраторы
- Проверка списка администраторов
- Рекомендации по безопасности
- Устранение неполадок
- Альтернативные методы

#### Быстрая инструкция
**Файл**: `ADMIN_SETUP_QUICK.md`

Краткая инструкция для быстрого старта:
- 3 простых шага
- Команды для разных ОС
- Ссылка на полную документацию

---

## Процесс настройки

### Шаг 1: Применение миграции

```bash
# Применить миграцию
npx prisma migrate dev --name add_is_admin_field

# Сгенерировать Prisma Client
npx prisma generate
```

### Шаг 2: Регистрация пользователя

Если пользователь еще не зарегистрирован:
1. Перейти на сайт
2. Зарегистрироваться через форму регистрации
3. Запомнить email

### Шаг 3: Назначение роли администратора

```bash
# Windows
.\scripts\make-admin.ps1 your-email@example.com

# Linux/Mac
./scripts/make-admin.sh your-email@example.com

# Или напрямую
npx ts-node scripts/make-admin.ts your-email@example.com
```

### Шаг 4: Проверка доступа

1. Войти в систему под учетной записью
2. Перейти на `/admin`
3. Увидеть административную панель

---

## Альтернативные методы

### Метод 1: Через SQL

```sql
-- Найти пользователя
SELECT id, email, name, "isAdmin" FROM "User" WHERE email = 'admin@fatos.pro';

-- Назначить администратора
UPDATE "User" SET "isAdmin" = true WHERE email = 'admin@fatos.pro';
```

### Метод 2: Через Prisma Studio

```bash
npx prisma studio
```

1. Открыть таблицу `User`
2. Найти пользователя
3. Изменить `isAdmin` на `true`
4. Сохранить

### Метод 3: Через переменную окружения (устаревший)

```env
# .env
ADMIN_EMAILS=admin1@fatos.pro,admin2@fatos.pro
```

**Примечание**: Этот метод больше не используется, но оставлен для обратной совместимости.

---

## Безопасность

### Реализованные меры

1. **Проверка на уровне базы данных**:
   - Поле `isAdmin` хранится в БД
   - Невозможно подделать через cookies или токены

2. **Двухуровневая проверка**:
   - Проверка на уровне страницы (UI)
   - Проверка на уровне API

3. **Логирование действий**:
   - Все действия администраторов логируются
   - Логи включают ID администратора и детали

### Рекомендации

1. **Ограничьте количество администраторов**
   - Назначайте роль только доверенным лицам
   - Регулярно проверяйте список

2. **Используйте сильные пароли**
   - Минимум 12 символов
   - Комбинация букв, цифр, символов

3. **Мониторинг**
   - Регулярно проверяйте логи в `/admin` → "Логи"
   - Следите за подозрительной активностью

---

## Проверка списка администраторов

### Через SQL

```sql
SELECT id, email, name, "createdAt" 
FROM "User" 
WHERE "isAdmin" = true 
ORDER BY "createdAt" DESC;
```

### Через Prisma Studio

```bash
npx prisma studio
```

Фильтр: `isAdmin = true`

---

## Снятие роли администратора

### Через SQL

```sql
UPDATE "User" SET "isAdmin" = false WHERE email = 'admin@fatos.pro';
```

### Через Prisma Studio

1. Открыть `npx prisma studio`
2. Найти пользователя
3. Изменить `isAdmin` на `false`

---

## Множественные администраторы

Можно назначить несколько администраторов:

```bash
npx ts-node scripts/make-admin.ts admin1@fatos.pro
npx ts-node scripts/make-admin.ts admin2@fatos.pro
npx ts-node scripts/make-admin.ts admin3@fatos.pro
```

Все администраторы имеют одинаковые права:
- Управление статьями
- Просмотр статистики
- Просмотр логов

---

## Устранение неполадок

### Ошибка: "Cannot find module '@prisma/client'"

```bash
npm install @prisma/client
npx prisma generate
```

### Ошибка: "Database connection failed"

Проверить `DATABASE_URL` в `.env`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/fatos_pro"
```

### Ошибка: "Column 'isAdmin' does not exist"

Применить миграцию:

```bash
npx prisma migrate dev --name add_is_admin_field
```

### Ошибка: "User not found"

Убедиться, что:
1. Email указан правильно
2. Пользователь зарегистрирован в системе

### Скрипт не запускается на Windows

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Скрипт не запускается на Linux/Mac

```bash
chmod +x scripts/make-admin.sh
```

---

## Статистика

- **Файлов создано**: 7
  - 1 миграция SQL
  - 3 скрипта (TypeScript, PowerShell, Bash)
  - 2 документации (полная, быстрая)
  - 1 summary
- **Строк кода**: ~400
- **Обновлено файлов**: 2 (schema.prisma, AdminService.ts)

---

## Что дальше

### Улучшения (опционально)

1. **UI для управления администраторами**:
   - Страница в админ панели
   - Список всех администраторов
   - Назначение/снятие роли через UI

2. **Роли и права**:
   - Разные уровни доступа
   - Супер-администратор
   - Модератор контента
   - Аналитик (только просмотр статистики)

3. **Двухфакторная аутентификация**:
   - Обязательная 2FA для администраторов
   - SMS или TOTP коды

4. **Аудит безопасности**:
   - Логирование входов администраторов
   - Уведомления о подозрительной активности
   - История изменений

---

## Заключение

Система управления ролями администратора полностью реализована и готова к использованию. Теперь можно:

1. ✅ Назначать администраторов через скрипт
2. ✅ Проверять права через поле в БД
3. ✅ Управлять статьями через админ панель
4. ✅ Просматривать статистику и логи
5. ✅ Безопасно контролировать доступ

Следующий шаг - использовать административную панель для создания статей и связывания их с нумерологическими расчетами.
