# 🚀 Деплой изменений на Vercel

## Что было изменено сегодня

### 1. Арканы (Cards of Day)
- ✅ Обновлены описания всех 22 арканов (4 времени суток для каждого)
- ✅ Исправлен алгоритм расчёта арканов (все 4 зависят от текущей даты)
- ✅ Добавлен автоматический поворот карты по времени суток
- ✅ Исправлена таблица расчёта имени (циклические значения 1-9)

### 2. Премиум услуги
- ✅ Обновлены описания (hook) для всех 7 услуг
- ✅ Обновлены цены и структура тарифов СТАРТ/ГЛУБОКИЙ
- ✅ Добавлена защита от ошибок в компоненте

## Шаги для деплоя

### Шаг 1: Коммит изменений в Git

```bash
# Проверьте статус изменённых файлов
git status

# Добавьте все изменения
git add .

# Создайте коммит с описанием
git commit -m "feat: обновление арканов и премиум услуг

- Обновлены описания всех 22 арканов с 4 временами суток
- Исправлен алгоритм расчёта арканов (все зависят от текущей даты)
- Добавлен автоматический поворот карты по времени суток
- Обновлены описания и цены премиум услуг
- Добавлена защита от ошибок в компонентах
- Исправлена таблица расчёта имени"
```

### Шаг 2: Отправьте изменения на GitHub

```bash
# Отправьте изменения в основную ветку
git push origin main
```

Или если у вас другая ветка:
```bash
git push origin master
```

### Шаг 3: Vercel автоматически задеплоит изменения

После `git push` Vercel автоматически:
1. Обнаружит новый коммит
2. Запустит сборку проекта
3. Задеплоит новую версию

**Процесс займёт 2-5 минут.**

### Шаг 4: Обновите базу данных на production

⚠️ **ВАЖНО**: SQL миграция для премиум услуг!

Откройте Neon SQL Editor (production база) и выполните:

```sql
-- Обновление премиум услуг
DELETE FROM "PremiumService";

INSERT INTO "PremiumService" (
  id, "serviceId", "titleRu", "titleEn", "descriptionRu", "descriptionEn",
  "hookRu", "hookEn", "priceBasicRUB", "priceBasicUSD", "priceFullRUB", "priceFullUSD",
  icon, color, "featuresBasic", "featuresFull", "buttonTextRu", "buttonTextEn",
  "isActive", "sortOrder", "createdAt", "updatedAt"
) VALUES
  -- Скопируйте весь INSERT из файла prisma/update-premium-services.sql
  ...
;
```

**Полный SQL скрипт находится в файле**: `prisma/update-premium-services.sql`

### Шаг 5: Проверьте деплой

1. Откройте https://vercel.com/your-project
2. Перейдите во вкладку "Deployments"
3. Дождитесь статуса "Ready" (зелёная галочка)
4. Нажмите на деплой и откройте "Visit"

### Шаг 6: Проверьте на сайте

Откройте https://fatos-pro.vercel.app/ru и проверьте:

**Арканы:**
- [ ] Расчёт показывает 4 карты (утро, день, вечер, ночь)
- [ ] Одна карта автоматически открывается по времени суток
- [ ] Описания содержат 4 раздела с emoji (🌅☀️🌇🌙)
- [ ] Каждая карта показывает только своё время суток

**Премиум услуги:**
- [ ] Отображаются все 7 услуг
- [ ] Новые описания (hook) под названием услуги
- [ ] Цены соответствуют новым значениям
- [ ] Два тарифа: СТАРТ (🌿) и ГЛУБОКИЙ (🔥)

**Переключение языка:**
- [ ] Переключите на английский (en)
- [ ] Все тексты переведены
- [ ] Цены отображаются в обеих валютах

## Альтернативный способ (если нет Git)

Если вы не используете Git, можно задеплоить через Vercel CLI:

```bash
# Установите Vercel CLI (если ещё не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте проект
vercel --prod
```

## Откат изменений (если что-то пошло не так)

Если после деплоя возникли проблемы:

1. Откройте https://vercel.com/your-project
2. Перейдите во вкладку "Deployments"
3. Найдите предыдущий рабочий деплой
4. Нажмите "..." → "Promote to Production"

Это вернёт сайт к предыдущей версии.

## Проверка логов

Если что-то не работает:

1. Откройте https://vercel.com/your-project
2. Перейдите во вкладку "Deployments"
3. Нажмите на последний деплой
4. Перейдите во вкладку "Build Logs" или "Function Logs"
5. Проверьте ошибки

## Файлы, которые были изменены

### Код (автоматически задеплоятся):
- `components/PremiumServices.tsx` - обновлены fallback данные
- `components/CardOfDay.tsx` - автоматический поворот карты
- `lib/arcana/arcanaCalculator.ts` - исправлен алгоритм
- `lib/validation/name.ts` - исправлена таблица имени

### База данных (нужно выполнить SQL вручную):
- `prisma/update-premium-services.sql` - обновление премиум услуг
- `prisma/insert-arcana-descriptions-full.sql` - обновление арканов (уже выполнено)

## Частые проблемы

### Проблема: Изменения не видны на сайте

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+R)
2. Проверьте, что деплой завершён (статус "Ready")
3. Проверьте, что SQL миграция выполнена на production базе

### Проблема: Ошибка при сборке

**Решение:**
1. Проверьте Build Logs в Vercel
2. Убедитесь, что нет TypeScript ошибок: `npm run build`
3. Проверьте, что все зависимости установлены

### Проблема: База данных пустая

**Решение:**
1. Убедитесь, что выполнили SQL миграцию на production базе
2. Проверьте переменную окружения DATABASE_URL в Vercel
3. Выполните SQL скрипт ещё раз

## Контрольный список

Перед деплоем убедитесь:
- [ ] Все файлы сохранены
- [ ] Нет TypeScript ошибок (`npm run build`)
- [ ] Локально всё работает (`npm run dev`)
- [ ] Git коммит создан
- [ ] Изменения отправлены на GitHub (`git push`)
- [ ] SQL миграция готова к выполнению на production

После деплоя проверьте:
- [ ] Деплой завершён успешно (статус "Ready")
- [ ] SQL миграция выполнена на production базе
- [ ] Сайт открывается без ошибок
- [ ] Арканы работают корректно
- [ ] Премиум услуги отображаются правильно
- [ ] Переключение языка работает

---

**Дата**: 2025-02-22
**Статус**: Готово к деплою
**Автор**: Kiro AI Assistant
